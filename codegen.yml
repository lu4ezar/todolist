overwrite: true
schema: "https://todo-gql-server.herokuapp.com/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.js:
    plugins:
      - "flow"
      - "flow-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
